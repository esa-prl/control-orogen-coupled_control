# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(coupled_controlTaskLib)
ADD_LIBRARY(${COUPLED_CONTROL_TASKLIB_NAME} SHARED 
    ${COUPLED_CONTROL_TASKLIB_SOURCES})
add_dependencies(${COUPLED_CONTROL_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${COUPLED_CONTROL_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${COUPLED_CONTROL_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${COUPLED_CONTROL_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${COUPLED_CONTROL_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${COUPLED_CONTROL_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${COUPLED_CONTROL_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${COUPLED_CONTROL_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${COUPLED_CONTROL_TASKLIB_HEADERS}
    DESTINATION include/orocos/coupled_control)

